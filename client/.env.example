# Backend API Base URL
# Leave empty or omit in production if served from the same domain (relative /api)
# For separate Vercel frontend + Render backend, set to: https://<your-render-backend-url>/api
VITE_API_URL=http://localhost:5000/api
